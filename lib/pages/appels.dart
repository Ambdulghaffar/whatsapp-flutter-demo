import 'package:flutter/material.dart';

class AppelsPage extends StatelessWidget {
  const AppelsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('Bienvenue dans la page Appels')),
    );
  }
}