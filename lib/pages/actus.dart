import 'package:flutter/material.dart';

class ActusPage extends StatelessWidget {
  const ActusPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('Bienvenue dans la page Actus')),
    );
  }
}